/**
 * @comments
 * Inline commments 
 * multiline comments
 * @NOTE comments arent detected by the compler
 */

//inline comments span a single line
//they are created by starting a line with two forward slashed(//)

//multiline comments span multiple lines
//the are created by starting a line with a forwad slash(/*) and an asterivck and ending the line with an asterick then a forwad slash(*/)

/**
 * This is a multiline comment
 */